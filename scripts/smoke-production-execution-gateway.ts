import assert from "node:assert/strict"; import { ProductionExecutionContract } from "../src/lib/production/ProductionExecutionContract"; import { ProductionExecutionGateway } from "../src/lib/production/ProductionExecutionGateway"; import { ProductionIntelligenceService } from "../src/lib/production/ProductionIntelligenceService"; import { intelligenceFixture } from "./production-intelligence-fixture";
const {snapshot,health}=intelligenceFixture(); const {plan}=ProductionIntelligenceService.derive(snapshot,health); const step=plan.steps.find(x=>x.id===plan.recommendedStepId)!; const req=ProductionExecutionContract.build("intelligence-project",plan,step,true); const validation=ProductionExecutionContract.validate(req,plan,plan.snapshotFingerprint); const result=ProductionExecutionGateway.dryRun(validation); assert.equal(result.status,"prepared"); assert.equal(result.operation?.serviceKey,"PipelineRunner.executeJobRetry"); assert.deepEqual(result,ProductionExecutionGateway.dryRun(validation)); assert.equal(ProductionExecutionGateway.execute().status,"rejected"); assert.ok(!JSON.stringify(result).includes("C:\\")); console.log("Sprint 96.5 execution gateway smoke: PASS (5 scenarios)");
