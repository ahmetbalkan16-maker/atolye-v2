import assert from "node:assert/strict"; import { ProductionExecutionContract } from "../src/lib/production/ProductionExecutionContract"; import { ProductionExecutionGateway } from "../src/lib/production/ProductionExecutionGateway"; import { ProductionExecutionJobContract } from "../src/lib/production/ProductionExecutionJobContract"; import { ProductionIntelligenceService } from "../src/lib/production/ProductionIntelligenceService"; import { intelligenceFixture } from "./production-intelligence-fixture";
const {snapshot,health}=intelligenceFixture(); const {plan}=ProductionIntelligenceService.derive(snapshot,health); const step=plan.steps.find(x=>x.id===plan.recommendedStepId)!; const request=ProductionExecutionContract.build("intelligence-project",plan,step,true); const dry=ProductionExecutionGateway.dryRun(ProductionExecutionContract.validate(request,plan,plan.snapshotFingerprint)); const job=ProductionExecutionJobContract.preview(request,dry,step); assert.equal(job.status,"prepared"); assert.deepEqual(job,ProductionExecutionJobContract.preview(request,dry,step)); assert.equal(job.idempotencyKey,request.idempotencyKey); assert.ok(!("snapshot" in job)); assert.ok(job.expectedOutputs.every(x=>x.persistence==="preview-only")); console.log("Sprint 96.6 execution job contract smoke: PASS (5 scenarios)");
