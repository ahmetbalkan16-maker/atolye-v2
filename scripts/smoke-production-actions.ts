import assert from "node:assert/strict"; import { ProductionActionEngine } from "../src/lib/production/ProductionActionEngine"; import { intelligenceFixture } from "./production-intelligence-fixture";
const {health}=intelligenceFixture(); const before=structuredClone(health); const one=ProductionActionEngine.recommend(health); const two=ProductionActionEngine.recommend(health); assert.deepEqual(one,two); assert.deepEqual(health,before); assert.equal(one[0].actionType,"retry-stage"); assert.equal(new Set(one.map(x=>x.id)).size,one.length); console.log("Sprint 96.1 production actions smoke: PASS (5 scenarios)");
